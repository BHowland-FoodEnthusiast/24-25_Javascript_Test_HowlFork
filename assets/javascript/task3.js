/* Put code for the task below */
function hideDir(){
    document.getElementById('basicDirections').innerText = 'I clicked it!'
}
/* Here we have a very basic script that simply IDs the p tag, and changes the text. */